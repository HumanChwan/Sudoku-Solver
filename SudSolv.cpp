#include <iostream>
#include <vector>
#define pb push_back

using namespace std;

int main(){
    const int r = 9, c = 9;
    vector<vector<int>> S(r, vector<int>(c));
    //for(auto& Si : S) for(auto& Sij : Si) cin>>Sij; //input(we could potentially hard code the input ig)
    
}